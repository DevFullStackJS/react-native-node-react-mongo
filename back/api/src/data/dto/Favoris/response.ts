export type FavorisResponseDTO = {
   name: string;
   user: string;
   produit: string;
  id: string;
}

/**
 * @typedef {object} FavorisResponseDTO
 * @property {string} name
 * @property {string} user
 * @property {string} produit
 */

