export type FavorisRequestDTO = {
   name: string;
   user: string;
   produit: string;
}

/**
 * @typedef {object} FavorisRequestDTO
 * @property {string} name
 * @property {string} user
 * @property {string} produit
 */

