export type DetailResponseDTO = {
   produit: string;
   utilisateur: string;
   nb: string;
   status: string;
  id: string;
}

/**
 * @typedef {object} DetailResponseDTO
 * @property {string} produit
 * @property {string} utilisateur
 * @property {string} nb
 * @property {string} status
 */

