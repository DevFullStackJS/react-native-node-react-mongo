export type GuideResponseDTO = {
   title: string;
   description: string;
   bg: string;
   image: string;
  id: string;
}

/**
 * @typedef {object} GuideResponseDTO
 * @property {string} title
 * @property {string} description
 * @property {string} bg
 * @property {string} image
 */

