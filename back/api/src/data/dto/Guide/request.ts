export type GuideRequestDTO = {
   title: string;
   description: string;
   bg: string;
   image: string;
}

/**
 * @typedef {object} GuideRequestDTO
 * @property {string} title
 * @property {string} description
 * @property {string} bg
 * @property {string} image
 */

