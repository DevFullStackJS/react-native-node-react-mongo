export type ContactRequestDTO = {
   icon: string;
   name: string;
   active: boolean;
   range: number;
}

/**
 * @typedef {object} ContactRequestDTO
 * @property {string} icon
 * @property {string} name
 * @property {boolean} active
 * @property {number} range
 */

