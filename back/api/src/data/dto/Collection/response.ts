export type CollectionResponseDTO = {
   produit: string;
   name: string;
  id: string;
}

/**
 * @typedef {object} CollectionResponseDTO
 * @property {string} produit
 * @property {string} name
 */

